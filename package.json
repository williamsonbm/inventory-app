{
  "name": "inventory-app",
  "version": "1.0.0",
  "private": true,
  "description": "Materials purchase planner — lumber, plates, hangers and LVL buy lists for a batch of jobs. No database.",
  "scripts": {
    "start": "node src/planner/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
